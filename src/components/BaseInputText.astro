---
const { label, required, class: classes } = Astro.props;
---

<div class="flex flex-col" class:list={classes}>
  <label for={label}>{label}</label>
  <input
    id={label}
    required={required}
    v-model="text"
    class="p-1 rounded outline-none mt-0 shadow shadow--press"
  />
</div>

<script></script>
